import { Link } from "@reach/router";
import React from "react";

export const Games = ({ children }) => {
  return (
    <div>
      {/* <h2><Link to="">Games</Link></h2> */}
      {children}
    </div>
  )
};

